import { Ripples } from '@uiball/loaders'

export default function Submitting({submitting}) {

    return (
        <Ripples 
        size={45}
        speed={2} 
        color="#6366f1" 
        />
    )
}